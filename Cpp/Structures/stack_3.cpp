#include <bits/stdc++.h>

using namespace std;

#define MAX 1000

Class Stack{
	int top;
public:
	int a[MAX];
	
	Stack(){ top = -1; }
	bool push(int x);
	int pop();
	bool isEmpty();
}

bool Stack::push(int x){
	if(top
}
